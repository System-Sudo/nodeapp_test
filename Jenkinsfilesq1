pipeline {

  environment {
    dockerimagename = "systemtesting48/nodeapp"
    dockerImage = ""
  }

  agent any

  stages {

   stage('Cloning Git') {
      steps {
        git([url: 'https://github.com/System-Sudo/nodeapp_test.git', branch: 'main', credentialsId: 'github'])

      }
    }

    stage('Build image') {
      steps{
        script {
          dockerImage = docker.build dockerimagename
        }
      }
    }

    stage('Pushing Image') {
      environment {
               registryCredential = 'Dockerhub'
           }
      steps{
        script {
          docker.withRegistry( 'https://registry.hub.docker.com', registryCredential ) {
            dockerImage.push("$BUILD_NUMBER")
             dockerImage.push('latest')
          }
        }
      }
    }

    stage('Deploy to K8s') {
      steps{
        script {
          sh "sed -i 's,TEST_IMAGE_NAME,systemtesting48/nodeapp:$BUILD_NUMBER,' deploymentservice.yml"
          sh "cat deploymentservice.yml"
          sh "kubectl --kubeconfig=/home/ec2-user/config get pods"
          sh "kubectl --kubeconfig=/home/ec2-user/config apply -f deploymentservice.yml"
        }
      }
    }
    
    
    stage('Remove Unused docker image') {
      steps{
        sh "docker rmi $imagename:$BUILD_NUMBER"
         sh "docker rmi $imagename:latest"

      }
    }

  }

}
